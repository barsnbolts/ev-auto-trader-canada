# Tauri Build Log

Single source of truth for what's shipped and what's queued in the
Tauri-standalone migration. Append one line per session.

## Shipped commits

| Date | HEAD | Phase | What landed |
|---|---|---|---|
| 2026-05-04 | `83d7f7b7` | A1 | Env-toggled `next.config.mjs` (`BUILD_TARGET=tauri` flips static export) |
| 2026-05-04 | `5aaad6a9` | A2-A11 | Full Tauri Phase A — 192 static pages, dataClient.ts, route migration, src-tauri scaffold, plugin-fs, icon, .app bundle (6.2MB arm64) |
| 2026-05-04 | `f44bb44d` | B1-B6 | Tauri IPC commands (run_refresh + run_verify_unit), RefreshModal + UnitVerifyChip wired into Nav + dossier |

## How to build the .app

```bash
cd ~/ev-auto-trader-canada
npm install
npm run icon                                          # only when icon source PNG changes
npm run static-meta                                   # bake snapshots + mtimes into data/meta-static.json
npx tauri build --target aarch64-apple-darwin
# .app at: src-tauri/target/aarch64-apple-darwin/release/bundle/macos/EV.trader CA.app
```

First build = 5-10 min (compiles ~400 Rust crates). Incremental rebuilds = 30-90 sec.

## How to verify it works

```bash
open "src-tauri/target/aarch64-apple-darwin/release/bundle/macos/EV.trader CA.app"
```

The window opens to `/`. Test sequence:
1. Click around all routes — homepage, inventory, compare, pick-a-model, dossier, dealer pages.
2. Verify the nav shows `⟳ Refresh` (only in the Tauri build, hidden on Vercel).
3. Click any unit row → dossier → click `⟳ Verify availability`. Should ping AutoTrader and report active/removed/challenged.
4. Click `⟳ Refresh` in nav → modal opens. (Don't click Start unless you want to actually run the refresh script.)

## Current state

- Branch: `claude/verify-environment-setup-oTu3S`
- Origin: `barsnbolts/ev-auto-trader-canada`
- HEAD: `f44bb44d`
- Working tree: cron-drift in `data/units.json` + `data/units-enrichment.json` is uncommitted (expected daily drift; commit when an organic data refresh lands)
- `data/meta-static.json`: regenerated by `npm run static-meta` before each Tauri build

## Phase queue

### Phase C (medium-tier next, ~12k tokens)

- C1: Dock badge — count of new units since last app open. Use `tauri-plugin-os` or Apple-native NSDockTile. Track `lastAppOpenAt` in localStorage; on launch count `units.firstSeen > lastAppOpenAt`.
- C2: `scripts/scrape_unit_gallery.py` — extract 6-15 photos per AutoTrader listing from JSON-LD. Write `data/unit-photos.json` keyed by unit ID.
- C3: `src/components/UnitPhotoGallery.tsx` — lightbox swipe + arrow keys + pinch-zoom on macOS trackpad.
- C4: Inventory row hover preview — second photo prefetched via existing ClientWarmup pattern.

### Phase D (cross-source intelligence, ~100k total)

- D0: Cross-source endpoint probe — see `docs/handoff/research/PHASE_D0_PROBE_*.md` (in progress as of 2026-05-04 by background research agent)
- D1-D5: Per-source scrapers (Kijiji, Leasebusters, Hyundai dealer, Kia dealer, Carfax) + `scripts/merge_cross_sources.py` + `data/cross-listings.json` + `<CrossSourceChip />` UI
- VIN-keyed merge architecture; VIN is the join key across sources

### Phase E (OEM API direct, ~60-80k, V8+)

- E0: Endpoint discovery on hyundaicanada.com + kia.ca dealer-locator JSON XHRs
- E1-E3: Per-store live inventory check via OEM feeds; surfaces as green dot on map pins

## Stop conditions (binding for next sessions)

- Tauri build fails twice on same root cause → halt, document, push, ping user
- Predeploy fails twice on same root cause → halt
- About to add code-signing, notarization, or DMG → halt (CLAUDE.md NO list)
- About to push to `main` → halt
- About to spend > $30 on Apify → halt

## Resume recipe

```
cd ~/ev-auto-trader-canada
git status --short
git log --oneline -5
cat docs/handoff/TAURI_BUILD_LOG.md          # this doc
cat docs/handoff/MAX_RUN_QUEUE_2026-05-04.md # Phase A queue (mostly done)
cat docs/handoff/research/PHASE_A_PREFLIGHT_2026-05-04.md
cat docs/handoff/research/PHASE_D0_PROBE_*.md 2>/dev/null  # if D0 ran
```

Phase A is shipped + verified. Phase B is shipped + verified. Phase C is the
next mechanical step; Phase D needs D0 research first.
