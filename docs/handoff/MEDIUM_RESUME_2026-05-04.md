# Medium Resume — 2026-05-04 (post-Max session)

**THIS IS THE ONE DOC TO READ AFTER COMPACTION.** Everything medium-tier needs to continue is here. Read top to bottom. No other doc required.

---

## Repo state at handoff

| Item | Value |
|---|---|
| Branch | `claude/verify-environment-setup-oTu3S` |
| HEAD | `7ddc467c` (will be 1 commit higher after this doc commits) |
| Origin | `barsnbolts/ev-auto-trader-canada` |
| Working tree | 3 modified files: `data/units.json`, `data/units-enrichment.json`, `data/meta-static.json` (all cron drift — leave them) |
| Built `.app` | `src-tauri/target/aarch64-apple-darwin/release/bundle/macos/EV.trader CA.app` (6.4MB arm64, 12:08 timestamp) |

```bash
# Verify state on resume:
cd ~/ev-auto-trader-canada
git status --short
git log --oneline -10
```

---

## What shipped this Max session (7 commits)

| HEAD | Phase | Output |
|---|---|---|
| `83d7f7b7` | A1 | Env-toggled `next.config.mjs` (`BUILD_TARGET=tauri` flips static export) |
| `5aaad6a9` | A2-A11 | Full Tauri Phase A — 192 static pages, dataClient.ts, route migration, src-tauri scaffold, plugin-fs, icon, .app bundle |
| `f44bb44d` | B1-B6 | Tauri IPC commands (`run_refresh` + `run_verify_unit`), RefreshModal + UnitVerifyChip wired |
| `24ff5abf` | docs | First TAURI_BUILD_LOG |
| `0f5c3ce3` | C1-C3 + D0 | Dock badge stub, photo gallery + scrape script, D0 cross-source endpoint research |
| `240bd8c1` | D-core scaffold | crossListings.ts schema + CrossSourceChip wired + Kijiji + Leasebusters scraper skeletons + merge script |
| `7ddc467c` | docs | Build log final update |

---

## Architecture map — what files exist and why

### Tauri shell (src-tauri/)

| File | Purpose | State |
|---|---|---|
| `Cargo.toml` | Rust deps: tauri 2 + plugin-fs + plugin-shell + serde | DONE |
| `build.rs` | Standard `tauri_build::build()` | DONE |
| `tauri.conf.json` | Window config + CSP (allows AT/Kijiji/Hyundai/Kia/Leasebusters/Carfax img + connect) + bundle config | DONE |
| `capabilities/main.json` | plugin-fs scope: `$HOME/ev-auto-trader-canada/data/**/*` | DONE |
| `src/main.rs` | Standard entrypoint | DONE |
| `src/lib.rs` | 3 commands: `set_dock_badge` (stub), `run_verify_unit`, `run_refresh` (with stdout/stderr streaming via `app.emit("refresh-log", line)`) | DONE except dock-badge body |

### Data layer

| File | Purpose | Notes |
|---|---|---|
| `next.config.mjs` | env-toggled: default = Vercel SSR, `BUILD_TARGET=tauri` = static export | Already shipped |
| `src/lib/dataClient.ts` | Static JSON imports mirroring `src/lib/data.ts` API. Imports from `@/data/...` | All loaders work, schema-validated |
| `src/lib/data.ts` | UNCHANGED — server-side path for Vercel | Kept |
| `src/lib/buyerContextServer.ts` | UNCHANGED — server-side cookie reader for Vercel | Kept |
| `src/lib/buyerContext.ts` | UNCHANGED — client hook | All client routes use this now |
| `data/meta-static.json` | Generated by `scripts/build_static_meta.py` — bakes mtimes + snapshots | Run before every Tauri build |

### Routes converted to client

7 routes were force-dynamic (cookie-reading or runtime-searchParams). All now work in static export:

| Route | Pattern | Dynamic param | Suspense |
|---|---|---|---|
| `src/app/page.tsx` | full client | — | — |
| `src/app/inventory/page.tsx` | full client + Suspense wrap | — | yes (useSearchParams) |
| `src/app/compare/page.tsx` | full client | — | — |
| `src/app/pick-a-model/compare/page.tsx` | full client + Suspense wrap | — | yes (useSearchParams) |
| `src/app/inventory/[id]/dossier/page.tsx` | thin server shell + `DossierClient.tsx` | yes — `generateStaticParams` returns 100 unit IDs | yes (in client child) |
| `src/app/dealer/[id]/page.tsx` | thin server shell + `DealerClient.tsx` | yes — `generateStaticParams` returns 80 dealer IDs | — |
| `src/app/pick-a-model/page.tsx` | server (force-dynamic dropped, no cookie usage) | — | — |

Routes that didn't need conversion (already force-static, no cookie): `intel`, `map`, `incentives`, `history`.

### New components

| File | Lines | Purpose | Tauri-only? |
|---|---|---|---|
| `src/lib/tauriRuntime.ts` | 65 | `isTauri` flag + safe wrappers for `invoke`/`listen`/`setDockBadge` | shared — guards on isTauri |
| `src/lib/crossListings.ts` | 106 | VIN-keyed lookup + fallbackKey (year\|make\|model\|trim\|kmBucket) for Leasebusters | shared |
| `src/components/RefreshButton.tsx` | 33 | Tauri-only Nav button → opens RefreshModal | yes (returns null in web) |
| `src/components/RefreshModal.tsx` | 147 | Live refresh log via `listen("refresh-log")` | yes |
| `src/components/UnitVerifyChip.tsx` | 100 | Dossier chip — calls `run_verify_unit`, renders 5 states | yes (web shows "desktop only") |
| `src/components/UnitPhotoGallery.tsx` | 114 | Lightbox with arrow-key + click navigation. Reads `data/unit-photos.json` | shared (web fine; gallery hidden when empty) |
| `src/components/CrossSourceChip.tsx` | 80 | "−$X on Kijiji" / "$Y/mo on Leasebusters" / null | shared |
| `src/components/DockBadgeSync.tsx` | 38 | Lifecycle component in layout, counts new units, calls setDockBadge | shared (no-op on web) |

### New scripts

| File | Status | Last run result |
|---|---|---|
| `scripts/build_static_meta.py` | WORKING | Bakes `data/meta-static.json` from snapshots |
| `scripts/verify_unit.py` | WORKING | Returns active/removed/challenged status per unit. Imperva sometimes walls AutoTrader; "challenged" state handles that |
| `scripts/scrape_unit_gallery.py` | SKELETON | Not yet primed; nightly cron will start filling `data/unit-photos.json` |
| `scripts/scrape_kijiji.py` | PARTIAL — see Phase D notes below | Returns 0 listings — make/model filter doesn't propagate to SSR |
| `scripts/scrape_leasebusters.py` | SKELETON — content is JS-rendered | Returns 0 listings — `.asp` returns shell only |
| `scripts/merge_cross_sources.py` | WORKING | Run produces `cross-listings.json` with 0 entries today (depends on the 2 scrapers above) |

### Bootstrapped empty data files

```
data/cross-listings.json     ← []
data/_kijiji_raw.json        ← {}
data/_leasebusters_raw.json  ← {}
data/unit-photos.json        ← {}
```

Don't delete — TS imports require them to exist.

---

## How to verify the .app

```bash
cd ~/ev-auto-trader-canada
open "src-tauri/target/aarch64-apple-darwin/release/bundle/macos/EV.trader CA.app"
```

Test sequence:
1. App opens to `/` — homepage renders, table loads after ~500ms (client-side load).
2. Click any inventory link → `/inventory` — table renders, slider works, region filter works.
3. Click any unit row → dossier opens.
4. Dossier header shows `⟳ Verify availability` chip — click it. Should say "active" or "challenged" within 3-5 sec.
5. Click `⟳ Refresh` in nav (only visible in Tauri build). Modal opens. **Don't click Start** unless you actually want to run the refresh script.
6. Print the dossier with Cmd+P — print stylesheet kicks in, Verify chip + photo gallery hidden.

If any of these fails, the build is broken. Check `docs/handoff/TAURI_BUILD_LOG.md` debugging playbook.

---

## How to rebuild the .app

```bash
cd ~/ev-auto-trader-canada
npm run static-meta                                   # regenerate meta-static.json
npx tauri build --target aarch64-apple-darwin
# .app at: src-tauri/target/aarch64-apple-darwin/release/bundle/macos/EV.trader CA.app
```

First clean build = 5-10 min. Incremental = 60-90 sec.

For dev mode with hot reload:

```bash
npm run dev:tauri    # tauri dev with frontend hot reload
```

---

## Phase D-core scraper status — TRUTH FOR MEDIUM

**Critical findings from Max session probing (do not redo this work):**

### kijiji.ca DNS / URL discoveries

- **`www.kijijiautos.ca` IS DEAD** (DNS SERVFAIL as of 2026-05-04). D0 research recommended this URL but it was outdated.
- **`www.kijiji.ca/b-cars-trucks/canada/.../c174l0`** is the working endpoint. Returns 1.3-1.4MB SSR HTML.
- **VINs ARE present** in `__NEXT_DATA__` JSON blob. Confirmed via `grep -oE '"vehicleIdentificationNumber":"[A-HJ-NPR-Z0-9]{17}"'` returns ~32-38 hits per page.
- **BUT** the make/model URL params (e.g. `kia-ev6` in path, or `attributeMap[carmake_s]=kia`) **do not propagate to SSR**. Plain curl gets a generic mixed-make/model feed regardless of URL filter.

**Why scrape_kijiji.py returns 0:**
The current code parses `<script type="application/ld+json">` blocks. There's only ONE such tag per page (organization metadata). The actual vehicle records live in `__NEXT_DATA__` (Next.js SSR data). My ad-hoc `__NEXT_DATA__` walker found 0 vehicles too — likely the data is heavily nested or uses different field names than expected.

**What medium needs to do (Phase D-core finishing):**

1. **Capture Kijiji's real GraphQL/REST endpoint via Chrome MCP**:
   ```
   - Open Chrome with claude-in-chrome MCP
   - Navigate to https://www.kijiji.ca/b-cars-trucks/ontario/kia/c174l9004
   - Check the Network tab for XHR requests filtering by make/model
   - The internal API likely returns clean JSON listings with VIN, price, km, dealer
   ```
2. **Update scrape_kijiji.py to call the captured endpoint** instead of scraping HTML.
3. **Run `python3 scripts/scrape_kijiji.py`** until `data/_kijiji_raw.json` has entries.
4. **Run `python3 scripts/merge_cross_sources.py`** to populate `data/cross-listings.json`.
5. **Restart `.app` or run `npm run dev`** — `CrossSourceChip` lights up automatically.

### Leasebusters discoveries

- **`leasebusters.com/en/lease-take-over-vehicle-gallery-results.asp?MakeID=Hyundai`** returns 33KB but it's a SHELL — Bootstrap CSS, ad scripts, no listing data. Content is fully JS-rendered.
- The `.asp` URL is misleading — it's not actually classic ASP, it's a Vue/SPA app behind a legacy URL.
- Plain curl is dead end. Need Chrome MCP probe to capture the real listing JSON XHR.

**What medium needs to do for Leasebusters:**

1. Same Chrome MCP probe as Kijiji — capture XHR after the gallery page loads.
2. The endpoint is likely `/api/...` or `/json/...` returning per-listing schema.
3. Confirm whether VIN appears post-account-creation (D0 noted Leasebusters anonymizes VIN until the lessee registers an interest). If a logged-in detail-page fetch DOES expose VIN, Leasebusters becomes a VIN-keyed source rather than fallback-key.
4. Update `scripts/scrape_leasebusters.py` to call the real API.

### Token estimate

Both scrapers fixed: ~15-25k tokens (dominated by Chrome MCP exploration + adapting the scrape body to the API response shape). The architecture (CrossSourceChip, merge script, schema) is already correct — only the scrapers need DOM iteration.

---

## Phase C dock badge — what's left

The `set_dock_badge` Rust command at `src-tauri/src/lib.rs:65` is currently a no-op stub:

```rust
#[tauri::command]
fn set_dock_badge(_count: Option<u64>) -> Result<(), String> {
    Ok(())
}
```

Tauri 2.11's `AppHandle::set_badge_count` doesn't exist. To wire the real binding:

1. Add `objc2 = "0.5"` and `objc2-app-kit = "0.2"` (or current versions) to `src-tauri/Cargo.toml` `[target.'cfg(target_os = "macos")'.dependencies]`.
2. Replace the stub body with:
   ```rust
   #[cfg(target_os = "macos")]
   unsafe {
       use objc2::{msg_send, runtime::Object, class};
       let app: *mut Object = msg_send![class!(NSApplication), sharedApplication];
       let dock_tile: *mut Object = msg_send![app, dockTile];
       let label: *mut Object = match count {
           Some(n) if n > 0 => {
               let s = format!("{}", n);
               // NSString init from rust string
               // ... msg_send! [class!(NSString), stringWithUTF8String: ...]
           }
           _ => std::ptr::null_mut(),
       };
       let _: () = msg_send![dock_tile, setBadgeLabel: label];
   }
   Ok(())
   ```
3. Token estimate: ~5k. Single Rust file change + recompile.

Frontend wiring is already in place (`DockBadgeSync.tsx`, `setDockBadge` in `tauriRuntime.ts`, mounted in `layout.tsx`). Just the Rust impl needs writing.

---

## Phase D-bis (deferred, ~95k)

OEM dealer feeds (Hyundai Click-to-Buy + Kia D2C Media). Per D0 research:

- **Hyundai**: every dealer has `<slug>.shop.hyundaicanada.com/inventory` Click-to-Buy subdomain. Cloudflare WAF returns 403 to plain curl. Needs Chrome MCP probe + headless browser scrape per dealer.
- **Kia**: per-dealer D2C Media (Solutions Medias 360) sites. ~100 ON dealers, each on their own domain. Mostly Cloudflare-fronted but generous. Per-dealer scrape config.
- VIN coverage: high on detail pages, varies on cards.

**Don't start Phase D-bis until Phase D-core (Kijiji + Leasebusters) is shipping data and proves cross-source value.**

---

## Phase E (deferred, V8+, ~60-80k)

OEM dealer API direct integration. Per-store live inventory check via `hyundaicanada.com` + `kia.ca` dealer-locator JSON XHRs.

**Don't start until Phase D core + D-bis are landed.**

---

## Cron drift — leave alone

`data/units.json` and `data/units-enrichment.json` show as modified in `git status`. This is the daily AutoTrader cron at 7am ET writing fresh inventory. **Don't commit these as part of feature work.** Either:
- Wait for the cron to run cleanly + commit as `data refresh: YYYY-MM-DD`
- Or run `git checkout data/units.json data/units-enrichment.json` to discard

`data/meta-static.json` is regenerated by `npm run static-meta` before each Tauri build. Also leave alone.

---

## Stop conditions (binding for medium)

- Tauri build fails twice on same root cause → halt, document in `TAURI_BUILD_LOG.md`, push, ping user
- Predeploy fails twice → halt
- About to add code-signing, notarization, DMG → halt (CLAUDE.md NO list)
- About to push to `main` → halt
- About to spend > $30 on Apify → halt
- Token-ROI gate: any single subagent dispatch > 70k tokens → halt

---

## Quick-fire FAQ for medium-tier

**Q: How do I run the dev server in the browser to test changes fast?**
A: `npm run dev` → http://localhost:3000. The `RefreshButton` and `UnitVerifyChip` show their "desktop only" / "not running in Tauri" states.

**Q: How do I run the dev server with the Tauri window?**
A: `npm run dev:tauri`. Hot reload works.

**Q: How do I make the homepage NOT a client component again?**
A: Don't. Static export needs all 7 force-dynamic routes to be client (or server-without-cookie). Going back breaks the .app build.

**Q: TypeScript error after pulling?**
A: Run `npx tsc --noEmit` from repo root. Known clean as of HEAD `7ddc467c`.

**Q: The CrossSourceChip never shows up. Is it broken?**
A: No — `data/cross-listings.json` is empty. Phase D-core scrapers haven't been fixed yet (Kijiji + Leasebusters need Chrome MCP probe — see above).

**Q: Why doesn't the dock badge update?**
A: Because `set_dock_badge` is a stub (see Phase C dock badge above). Frontend calls it correctly; Rust no-ops.

**Q: The Verify chip says "challenged" — bug?**
A: Not a bug. AutoTrader fronts Imperva; sometimes plain HTTP GETs return a 200 with a placeholder. The chip honestly reports that uncertain state instead of false-positive "active". Click the AutoTrader link in the dossier to verify manually.

**Q: How does plugin-fs scope work?**
A: Capabilities at `src-tauri/capabilities/main.json` allow `$HOME/ev-auto-trader-canada/data/**/*`. If you move the repo, edit that scope (and `REPO_REL` in `src-tauri/src/lib.rs`).

**Q: Where's the Vercel preview URL?**
A: `barsnbolts-projects/ev-auto-trader-canada` on Vercel. Default branch `claude/verify-environment-setup-oTu3S`. Auto-deploys on push.

---

## File-by-file change inventory (Max session)

```
M  next.config.mjs                                    (env-toggle, A1)
A  src/lib/dataClient.ts                              (A2)
A  src/lib/tauriRuntime.ts                            (B1)
A  src/lib/crossListings.ts                           (D-core)
M  src/app/page.tsx                                   (A4 client conversion)
M  src/app/inventory/page.tsx                         (A4 + Suspense)
M  src/app/compare/page.tsx                           (A4)
M  src/app/pick-a-model/page.tsx                      (drop force-dynamic only)
M  src/app/pick-a-model/compare/page.tsx              (A4 + Suspense)
M  src/app/inventory/[id]/dossier/page.tsx            (server shell)
A  src/app/inventory/[id]/dossier/DossierClient.tsx   (client child)
M  src/app/dealer/[id]/page.tsx                       (server shell)
A  src/app/dealer/[id]/DealerClient.tsx               (client child)
M  src/app/layout.tsx                                 (DockBadgeSync added)
M  src/components/InventoryTable.tsx                  (CrossSourceChip wired)
M  src/components/Nav.tsx                             (RefreshButton added)
A  src/components/RefreshButton.tsx                   (B6)
A  src/components/RefreshModal.tsx                    (B2)
A  src/components/UnitVerifyChip.tsx                  (B5)
A  src/components/UnitPhotoGallery.tsx                (C3)
A  src/components/CrossSourceChip.tsx                 (D-core)
A  src/components/DockBadgeSync.tsx                   (C1)
A  scripts/build_static_meta.py                       (A3)
A  scripts/verify_unit.py                             (B3)
A  scripts/scrape_unit_gallery.py                     (C2)
A  scripts/scrape_kijiji.py                           (D-core skeleton — needs Chrome MCP)
A  scripts/scrape_leasebusters.py                     (D-core skeleton — needs Chrome MCP)
A  scripts/merge_cross_sources.py                     (D-core)
A  src-tauri/Cargo.toml + build.rs + main.rs          (A6)
A  src-tauri/src/lib.rs                               (B1+B4 + C1 stub)
A  src-tauri/tauri.conf.json                          (A6)
A  src-tauri/capabilities/main.json                   (A6.5 plugin-fs scope)
A  src-tauri/icons/                                   (A8 — 60+ generated icons)
A  data/meta-static.json                              (A3 generated)
A  data/cross-listings.json                           (D-core bootstrap, empty)
A  data/_kijiji_raw.json                              (D-core bootstrap, empty)
A  data/_leasebusters_raw.json                        (D-core bootstrap, empty)
A  data/unit-photos.json                              (C2 bootstrap, empty)
A  assets/ev-monogram-1024.png + .svg                 (icon source)
M  package.json + package-lock.json                   (tauri deps + sharp + new scripts)
A  docs/handoff/MAX_RUN_QUEUE_2026-05-04.md           (Max session prep doc)
A  docs/handoff/TAURI_BUILD_LOG.md                    (running build log)
A  docs/handoff/research/PHASE_A_PREFLIGHT_2026-05-04.md  (A0-bis findings)
A  docs/handoff/PHASE_D0_CROSS_SOURCE_PROBE_2026-05-04.md (D0 findings)
A  docs/handoff/MEDIUM_RESUME_2026-05-04.md           (this doc)
```

---

## Context window budget for medium

Per MEMORY.md the user prefers token-ROI discipline. Estimated remaining work cost:

| Task | Tier | Tokens | Why |
|---|---|---|---|
| Phase D-core scraper DOM iteration | medium | 15-25k | Chrome MCP probe + adapt body to JSON API |
| Phase C dock badge NSDockTile | medium | 5k | Single Rust file with objc2 unsafe block |
| Phase D-bis Hyundai scrape | medium-high | 50k | WAF + per-dealer crawl + headless browser |
| Phase D-bis Kia scrape | medium-high | 45k | D2C Media per-dealer scrape config |
| Phase E0 + E1-E3 | extra-high → medium | 60-80k | OEM API endpoint discovery + integration |

**Recommend next medium session:** start with Phase C dock badge (small, mechanical, ships an IPC win) → then Phase D-core Chrome MCP probe (the unblocker) → defer D-bis until D-core proves data flow.

---

## Verification before declaring "medium session done"

```bash
cd ~/ev-auto-trader-canada
npx tsc --noEmit                                              # exit=0
npm run predeploy                                             # exit=0 (Vercel build)
BUILD_TARGET=tauri npm run build:tauri:web                    # exit=0 (static export)
npx tauri build --target aarch64-apple-darwin                 # exit=0 (.app rebuild)
open "src-tauri/target/aarch64-apple-darwin/release/bundle/macos/EV.trader CA.app"
# Smoke-test: 5 routes load, Verify chip works, Refresh modal opens, no console errors
git status --short                                            # only cron drift
git log --oneline -5                                          # your new commits at top
```

If all green, push + update `TAURI_BUILD_LOG.md` Shipped commits table.

---

## Final word

Phase A + B are PRODUCTION READY. Phase C scaffolds (badge stub + photo gallery) and Phase D scaffolds (chip + merge + skeletons) all typecheck, all build, all included in the .app. The only "needs work" items are clearly delimited:

- Phase C dock badge: stub Rust impl (~5k)
- Phase D-core scrapers: skeletons that need Chrome MCP probe (~15-25k)

Everything else is shipped, tested, in the .app. Resume at any of the above whenever you have the bandwidth.

---

## After the queue empties — autonomous mode

**Read `docs/handoff/AUTONOMOUS_MODE.md` and follow it.**

Once `docs/handoff/TODO_INDEX_2026-05-04.md` items are all shipped (or
explicitly deferred per stop conditions), do NOT idle. Switch to the
autonomous mode protocol — Tier 1-5 ladder of polish, verification,
testing, and creative wins. The user explicitly authorized "keep working
until I say stop" on 2026-05-04.

The protocol is self-contained, has hard rules (CLAUDE.md NO list still
binds), and a re-prime mechanism (when the ladder runs dry, generate the
next batch). Subagent dispatch is allowed (default sonnet). No hard
per-loop or per-session token cap, but checkpoint every ~100k cumulative
into `docs/handoff/AUTONOMOUS_LOG.md`.

**Stop conditions only:** user message in chat, hard build failure twice
on same root cause, or about to violate CLAUDE.md NO list.

---

## Preview window — verification recipe (use the Claude Preview MCP)

The Claude Code app on macOS supports an embedded preview pane via the
`Claude_Preview` MCP. Launch config lives in `.claude/launch.json` —
already wired with two configs:

| Name | Port | Command |
|---|---|---|
| `Next.js dev` | 3000 | `npm run dev` |
| `Next.js production preview` | 3000 | `npm run start` |

Recipe to use it from the medium session:

```
# 1. Verify shell cwd is the EV trader project (not EV dashboard).
cd ~/ev-auto-trader-canada

# 2. Start the preview server.
preview_start name="Next.js dev"
# Returns serverId. Note it; pass to subsequent preview_* calls.

# 3. Sanity-check it's running.
preview_logs serverId="<id>" lines=10
# Expect: "Ready in <Xms>" + "Local: http://localhost:3000"

# 4. Take a screenshot of the homepage.
preview_screenshot serverId="<id>"

# 5. Inspect a chip's actual computed style.
preview_inspect serverId="<id>" selector=".chip-accent" styles=["color","background"]

# 6. Click a Dossier link and verify route navigation.
preview_click serverId="<id>" selector="a[href*='/inventory/'][href*='/dossier']"
preview_snapshot serverId="<id>"
```

The preview server is reused across calls — no need to restart between
verifications. `preview_stop serverId="<id>"` kills it when done.

**If two projects' preview servers collide on port:** the EV dashboard
project (`/Users/ianmcadam/Documents/Claude/Projects/EV dashboard`)
binds Vite to port 1420; ev-auto-trader-canada binds Next.js to port
3000. They don't collide. But running two `preview_start` calls back to
back may confuse `preview_list` — call `preview_list` first, reuse any
existing serverId before launching new ones.
