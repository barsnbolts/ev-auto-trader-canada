# A6 pre-flight audit (2026-05-02)

## Summary
- PASS: 17
- WARN: 4
- FAIL: 2 ← plan cannot proceed without fix

---

## Findings

### 1. Schema invariants

- [PASS] `data/dealers.json`: 80 records, all pass `DealerSchema`
- [PASS] `data/units.json`: 100 records, all pass `InventoryUnitSchema`
- [PASS] `data/incentives.json`: 27 records, all pass `IncentiveSchema`
- [PASS] `data/specs.json`: 20 records, all pass `SpecSchema`
- [PASS] `data/taxes-and-fees.json`: passes `TaxesAndFeesSchema`
- [PASS] `data/market-intel.json`: passes `MarketIntelSchema`
- [PASS] `data/used-listings.json`: 72 records, all pass `UsedListingSchema`
- [PASS] `data/snapshots/*.json`: 4 snapshot files, all pass `SnapshotSchema`
- [PASS] `data/oem-pricing.json` (no Zod schema): nested shape `{ lastVerified, source, notes, msrp: { [model]: { [trim]: { value, lastVerified, source, staleSince } } } }` is consistent across all model/trim entries
- [WARN] `data/vehicle-images.json` (no Zod schema): structure is consistent (`{ lastVerified, source, notes, images: { [model]: { url, license, commonsPage, alt } } }`), all 5 entries present for all MODELS. **However, 4 of 5 URLs will 404 — see FAIL #2 below.**
- [PASS] `data/units-enrichment.json` (no Zod schema): 100 entries, consistent fields (`daysOnLot`, `daysOnLotFirstSeen`, `daysOnLotSource`; 8 entries also have `fetchedAt`, `phone`, `streetAddress`, `vin`). All values type-consistent.

**Note on Zod stripping:** `InventoryUnitSchema` is a plain `z.object()` (strip mode). Fields present in `units.json` but absent from the schema — `daysOnLotFirstSeen`, `daysOnLotSource`, `fetchedAt`, `phone`, `streetAddress` — are stripped when `loadUnits()` calls `schema.parse()`. This is **by design**: `daysOnLot` is schema-defined and preserved; the stripped fields are either UI-irrelevant or handled via the separate enrichment/dealer backfill path. Confirmed correct.

---

### 2. Cross-reference integrity

- [PASS] `unit.dealerId` → `dealers.json`: all 100 unit dealerIds resolve to a known dealer (80 dealers, no orphan references)
- [WARN] Unit tuples → `specs.json`: 26 of 100 units have no matching `(model, year, trim, drivetrain)` tuple in `specs.json`. These units will show `rangeKm = null` in the inventory table. Not a crash — `rangeByUnitId[u.id] = s?.rangeKm ?? null` handles it gracefully — but 26% of units silently display no range. Unmatched groups:
  - `EV6|2025|Trim unknown (Kia EV6 W/PREMIUM PKG - Leather Seats)|RWD` — 1 unit (asking-fallback, scrape artifact)
  - `Ioniq5|2025|N|RWD` — 2 units (Ioniq 5 N is AWD-only; drivetrain mislabeled?)
  - `Ioniq5|2026|Preferred RWD|RWD` — 1 unit (no 2026 "Preferred RWD" in specs; specs have "Preferred RWD Long Range")
  - `Ioniq6|2025|Preferred RWD|RWD` — 6 units (specs have "Preferred RWD Long Range", not bare "Preferred RWD")
  - `EV9|2026|GT-Line AWD|AWD` — 4 units (specs only have 2025 GT-Line AWD, not 2026)
  - `EV9|2026|Light Long Range RWD|RWD` — 1 unit
  - `EV9|2026|Trim unknown (Kia EV9)|RWD` — 2 units (scrape artifacts)
  - `EV9|2026|Land Long Range AWD|AWD` — 8 units (specs only have 2025)
  - `EV9|2026|Light RWD|RWD` — 1 unit
- [PASS] `incentive.appliesTo.models` values: all 27 incentives reference only MODELS-constant values (or omit `models` to mean "all"). No stale model names.
- [PASS] `oem-pricing.json` coverage: 55 units are `msrpSource = "spec-lookup"`, 42 are `"default-table"` (all 42 have a matching entry in `oem-pricing.msrp`), 3 are `"asking-fallback"`. Zero `default-table` units are missing from `oem-pricing`.
- [WARN] 3 `asking-fallback` units have `msrp === dealerAskingPrice` (set from asking price, no OEM MSRP found). For these units, `priceVsMsrpScore()` returns 0.5 (neutral) and `dealerAdjustment = 0`, making deal score unreliable. The schema comment at `InventoryUnitSchema.msrpSource` correctly documents this; the inventory page should filter these from "Top Deals." Units: `u-at-ffe7ec19` (EV6 unknown trim), `u-at-3df8e805` (EV9 unknown trim), `u-at-8f937847` (EV9 unknown trim, MSRP $81,995 = GT-Line or Land guess).

---

### 3. otdPaths flow

100 units checked. **0 with finance path, 0 with lease path** — this is the expected state. `incentives.json` contains 0 `finance_promo` and 0 `lease_promo` entries; `bestPromo()` returns `undefined` for both scopes; `computeFinanceOtd` and `computeLeaseOtd` both return `null`; `data.ts` spreads nothing into `otdPaths`. All 100 units have `otdPaths = { cash: otdBreakdown }` only.

- [PASS] `otdPaths.cash` always present (guaranteed by data.ts construction)
- [PASS] `otdPaths.cash === otdBreakdown` (same object reference at data.ts:302 — `cash: otdBreakdown` — so `otdPaths.cash.total === unit.otdBreakdown.total` is a tautology)
- [PASS] `otdPaths.finance === undefined` for all 100 units (expected; no finance promos)
- [PASS] `otdPaths.lease === undefined` for all 100 units (expected; no lease promos)

Finance/lease math (PMT + money-factor) is in `scoring.ts` but untriggered. No spot-checks possible — none applicable.

---

### 4. Enrichment merge (M16) — daysOnLot

- [PASS] All 100 units in `units-enrichment.json` have `daysOnLot` set; all 100 corresponding `units.json` entries have `daysOnLot` set with matching values. The M16 merge bug (enrichment re-keyed but not written back) is confirmed fixed: `build_units_from_at.py` now bakes `daysOnLot` into `units.json` at build time, and `loadUnits()` in `data.ts` re-applies it at runtime via `e.daysOnLot ?? u.daysOnLot` (no-op since values match, but safe). Zero units have enrichment `daysOnLot` without a corresponding units.json `daysOnLot`.
- [PASS] Dealer phone/streetAddress backfill path in `loadDealers()`: 8 enrichment entries have `phone`/`streetAddress`; `loadDealers()` correctly reads these from `units-enrichment.json` (not from stripped `InventoryUnit` objects). Backfill will fire correctly for the 8 dealers whose enrichment entry is the first unit seen for that dealer.

---

### 5. Routes smoke-test

- [PASS] `GET /` → HTTP 200, no runtime error markup
- [PASS] `GET /inventory` → HTTP 200, 100 unit IDs (`u-at-*`) present in rendered output, no runtime exceptions
- [PASS] Dev server starts cleanly in ~1.4s (`next dev` on Next.js 15.5.15, port 3002)
- [PASS] TypeScript clean: `npx tsc --noEmit` exits 0, zero type errors

Note: The RSC stream for `/inventory` contains a `404 / "This page could not be found"` fragment but this is embedded in the RSC error-boundary component bundle (Next.js internal `__next_builtin__not-found.js`), not an actual page failure. Inventory data renders correctly.

---

### 6. New file integrity

- [PASS] `src/lib/usedListingsLinks.ts`: TypeScript clean (confirmed by `tsc --noEmit`). Logic test: `usedListingsFor("Ioniq5", "ON")` produces `https://www.autotrader.ca/cars/hyundai/ioniq+5/on/?rcp=20&rcs=0&sts=Used` (well-formed). All 15 combinations (5 models × 3 provinces sampled) produce well-formed `https://` URLs with no `undefined` segments. Kia make code `a64` vs Hyundai `a54` confirmed consistent.
- [FAIL] `data/vehicle-images.json`: **4 of 5 URLs will 404.** Wikipedia Commons uses MD5-based directory hashing (`/first_hex_char/first_two_hex_chars/filename.ext`). MD5 hash verification:

  | Model | URL hash | Computed correct hash | Status |
  |---|---|---|---|
  | Ioniq6 | `/e/ee/` | `/e/ee/` | PASS |
  | Ioniq5 | `/3/38/` | `/7/76/` | **FAIL — 404** |
  | Ioniq9 | `/x/xx/` | `/2/2f/` | **FAIL — 404** (`x` is not hex) |
  | EV6 | `/x/xx/` | `/d/da/` | **FAIL — 404** (`x` is not hex) |
  | EV9 | `/x/xx/` | `/b/b7/` | **FAIL — 404** (`x` is not hex) |

  `vehicle-images.json` is not currently imported by any `src/` file, so this does not cause a runtime error today. When a component consumes it (planned for Phase 3), 4 broken `<img>` tags will silently fail to render, showing alt text only. **The file should not be consumed until the hashes are corrected.**

---

## Required fixes before tier-drop

1. **[FAIL] `data/vehicle-images.json` — 4 broken Wikipedia Commons URL hashes.** Correct the directory hash segment for Ioniq5, Ioniq9, EV6, EV9. The correct paths (computed from MD5 of filename) are:
   - Ioniq5: change `/3/38/` → `/7/76/`
   - Ioniq9: change `/x/xx/` → `/2/2f/`
   - EV6: change `/x/xx/` → `/d/da/`
   - EV9: change `/x/xx/` → `/b/b7/`
   Verify against `https://commons.wikimedia.org/wiki/File:<filename>` before writing. No code change needed; file is not yet wired to UI.

2. **[FAIL (latent)] `data/specs.json` — 26 units (26%) have no matching spec tuple, silently rendering `rangeKm = null`.** This is not a crash today, but is a data completeness bug that degrades the primary value prop (range display). Highest-impact gaps: EV9 2026 trims (15 units total missing specs), Ioniq6 2025 "Preferred RWD" (6 units), Ioniq5 N trim drivetrain mismatch (2 units labeled RWD, EV9 N is AWD-only). Fix: add missing spec rows for MY2026 EV9 trims + verify Ioniq5 N drivetrain label.

---

*Audit performed 2026-05-02. Read-only — no source files modified.*
