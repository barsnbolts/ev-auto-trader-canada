# t5 — spec-writing probe

**Feature request (one line):** add a 'compare two trips' button in the Trip Plan tab so the user can see two routes side-by-side with their charging plans.

Write a spec for this feature using the project's six-section template. Save the spec to specs/cluster-K/two-trip-compare.md.
