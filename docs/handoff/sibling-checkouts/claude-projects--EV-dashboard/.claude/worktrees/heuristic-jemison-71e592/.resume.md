# Resume here — EV Dashboard

*Volatile file — updated by `scripts/queue.py` and `scripts/milestone.py` each cluster.*

## Where we are

**Clusters A–F + H + N v2 — COMPLETE.** Worktree branch `claude/heuristic-jemison-71e592` fully committed. **51 vehicles across 23 brands.** Ready to merge into master.

Cluster N v2 landed 2026-04-25 in a single Opus-4.7/high session: D-01 verification pass (51 field patches across 24 vehicles + 1 deletion), D-04 expansion (27 new vehicles 24→51, 12 new brands), F-09 cache-warmer, F-11 incentive disclaimer, P-02 audit (no holdouts), I-08 health badge, I-09 changelog generator, I-10 snapshot capture, I-13 memory rsync, I-06 quiet.py, I-04 research_client.py.

**Next session pickup:** queue.py top items — likely Cluster J/K work (MapLibre Metal swap, OSRM trip planner) or run `python3 scripts/refresh_used_market.py` after Ian sets `EXA_API_KEY` in `.env.local` to populate F-09 cache.

All clusters complete:

All clusters complete:
- ✅ Phase 1 MVP (20-vehicle seed + filters + compare view)
- ✅ Cluster A — Foundation (ritual, memory, hooks, Vitest, validate.py, smoke.sh, Zod)
- ✅ Cluster B — Data rigor (thermal profiles all 20 vehicles, Exa B3 pass, Juniper B4, drift monitor B5)
- ✅ Cluster C — Physics + cost + bugs (F-02 cost/100km+TOU, B-01 persist middleware, F-03 tooltips, C4 rollupConfidence fix, C6 UI polish)
- ✅ Cluster D — Map + range rings (Rust thermal plugin + Leaflet map + DCFC pins + tab bar)
- ✅ Cluster E — Trip planning + used-market + degradation (4-tab CompareView + Rust greedy solver + UsedMarketPanel + FeatureFlag iZEV scaffold)
- ✅ Cluster F — Mom mode + accessibility + design sweep + Tauri package
- ✅ Cluster H — Token ROI + autonomy + roadmap v2 (caveman everywhere, debug infra, drive_app, playbooks)
- ✅ Cluster N v2 — Single-Opus-high set-and-forget batch: D-01 verify + D-04 expansion (51 vehicles, 23 brands) + F-09 cache + F-11 + P-02 + I-04/06/08/09/10/13 infra. iZEV weekly drift cron registered.

## Cluster N v2 summary (2026-04-25)

**Delivered:**
- **51 vehicles, 23 brands** in seed.json (was 24/11). New brands: subaru, toyota, gmc, cadillac, mercedes-benz, lucid, porsche, rolls-royce, audi, bmw, volvo, honda, acura.
- **D-01**: 51 field patches across 24 existing vehicles, 1 deletion (Tesla Model Y Juniper LR RWD US-only). Bare-MSRP convention applied.
- **D-04**: 27 new CA-market vehicles. 4 explicitly skipped (Porsche Taycan base RWD, BMW i5 eDrive40 RWD, Honda Prologue FWD, Tesla Model Y RWD — all not sold in Canada).
- **F-09 scaffolding**: `scripts/refresh_used_market.py` — pre-warms `public/used-market-cache.json` from seed (38 unique brand-model pairs). UsedMarketPanel.tsx already client-side-wired. Run once Ian sets `EXA_API_KEY`.
- **F-11**: `src/lib/incentives.ts` — `incentivePaused = true` constant + paused disclaimer wired into CompareView federal_izev row.
- **I-09**: `scripts/changelog.py` auto-generates `CHANGELOG.md` from git log (per-cluster grouping). Wired into milestone.py step 14.
- **I-08**: `scripts/queue.py` injects 4-check health badge (🟢 git/vitest/validate/metrics-fresh) at top of `SESSION_SUMMARY.md`.
- **I-10**: `scripts/snapshot.py` Chromium-headless screenshot per milestone, graceful skip if no browser. Step 15.
- **I-13**: milestone.py step 16 rsyncs `~/.claude/projects/.../memory/` to `~/.claude/backups/ev-dashboard_<ts>/`.
- **I-06**: `scripts/quiet.py` RTK-clone output compressor (tsc/vitest/cargo/git specializations). Auto-wired into milestone.py.
- **I-04**: `scripts/research_client.py` Exa+Apify Python bindings + `.env.local` loader. Graceful no-key fallback.
- **iZEV cron**: weekly Wed 9:09am via scheduled-tasks MCP — logs program status to `logs/izev_status_check.md`.
- **PreToolUse hint hook**: `.claude/settings.json` nudges to wrap raw verbose Bash with `scripts/quiet.py`.
- **Permission allowlist**: 35 entries in `.claude/settings.json` (Bash + Exa MCP + Preview MCP) — fewer prompts during future sessions.

**Deferred (not in this batch):**
- F-05/06/07/08 (MapKit + OCM + ABRP keys — Ian-only)
- L-01/L-02 (Apify + Exa keys — graceful fallback present)
- H8c (worktree merge to master — Ian-only)
- Cluster J/K/M (architectural — next Opus session)
- MY26 arrivals batch (Polestar 4, Kia EV3, Subaru Trailseeker EV, Hyundai IONIQ 9 — when on CA dealer lots)

## Cluster F summary

- **F1**: `src/lib/labels.ts` BiLabel system + Plain/Geek toggle (persisted in store) — works in VehicleRow + CompareView
- **F2**: Basic accessibility done (global focus rings, aria-pressed, aria-label, aria-hidden). Deep audit paused — **needs Ian's input on mom's specific accessibility needs** (low vision? motor? cognitive?)
- **F3**: Chrome-MCP regression sweep — all tabs verified, Leaflet double-init error fixed, slider→rings confirmed
- **F4**: `npm run tauri build` succeeded — 8.1MB ARM64 binary at `src-tauri/target/release/bundle/macos/EV Dashboard.app`. Drag to `/Applications` to install. DMG creation failed (requires UI access) but .app is fully functional.
- **F5**: `docs/user_guide.md` written + `CreditsPanel` component in CompareView footer
- **F6**: Tailwind tokens locked (transitionDuration, fontSize scale), global focus-visible ring in `index.css`
- **F7**: Code review complete — all 🔴 and 🟡 findings resolved
- **F8**: Milestone committed. Merge blocked by uncommitted changes in master — Ian should handle manually.

## Merge instructions for Ian

The worktree branch has all the good stuff. To merge into master:

```bash
cd "/Users/ianmcadam/Documents/Claude/Projects/EV dashboard"
# Option A: commit or stash master changes first, then merge
git add -A && git commit -m "wip: master state before F-cluster merge"
git merge --no-ff claude/heuristic-jemison-71e592

# Option B: hard reset master to the worktree (throws away master's uncommitted changes)
# git checkout master
# git reset --hard claude/heuristic-jemison-71e592
```

## Optional next: Cluster G (SwiftUI rewrite)

After using the app for a few weeks — does it feel like a Mac app, or a website in a window? If the latter, Cluster G is the SwiftUI native rewrite path. Ian decides.
